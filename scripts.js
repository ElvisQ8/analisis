// Función para mostrar los subprocesos de un proceso
function showProcess(processId) {
    alert("Mostrando los subprocesos de " + processId);
    // Aquí agregaríamos la lógica para mostrar los subprocesos
    // Puedes usar animaciones o transiciones para mostrar los subprocesos de manera secuencial
}

// Función para abrir el modal del PDF
function openModal() {
    document.getElementById('pdfModal').style.display = 'flex';
}

// Función para cerrar el modal del PDF
function closeModal() {
    document.getElementById('pdfModal').style.display = 'none';
}
